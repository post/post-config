{
  "extends": ["angular"],
  "wildcards": {
    "merge": ["^(Merge pull request)|(Merge (.*?) into (.*?)|(Merge (.*?) branch (.*?))$)"],
    "prerelease": ["^(\\d*.\\d*.\\d*)(?:-)(?:(\\d.{0,3})|alpha|beta|rc)(.\\d*)?$"]
  }
}
